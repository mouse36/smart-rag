# Core Dependencies (always required)
# Build Tools (required for package installation)
setuptools>=65.0.0
wheel>=0.38.0

# Web Framework
flask==3.1.1
flask-cors==6.0.1

# HTTP Requests
requests==2.32.4

# Payment Processing
stripe==12.4.0

# Authentication
PyJWT>=2.10.1
bcrypt==4.1.2

# Firebase Admin (for authentication and database)
firebase-admin==7.1.0

# Data Processing
pandas>=2.1.0

# Development & Debugging
python-dotenv==1.1.1
psutil==7.0.0

# AI/ML Libraries (only required when API_CALLS_ENABLED=true)
# These libraries are used for embeddings, vector search, and DeepSeek API integration
# NOTE: These are heavy libraries that may cause memory issues on limited environments
# Consider using requirements-core.txt for deployment without vector search
sentence-transformers==3.3.0
faiss-cpu==1.11.0.post1
numpy>=1.26.0
# Use CPU-only torch for smaller memory footprint
torch>=2.1.0
--extra-index-url https://download.pytorch.org/whl/cpu

# Optional: GPU support (uncomment if you have CUDA)
# faiss-gpu==1.11.0.post1  # replace faiss-cpu with this for GPU support
# torch==2.0.1+cu118  # replace torch with CUDA version
