[phases.setup]
nixPkgs = [
  "python311",
  "python311Packages.pip"
]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "echo 'Build completed successfully'"
]

[start]
cmd = "python run.py"


