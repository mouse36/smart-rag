# Root requirements.txt for Nixpacks detection
# This file helps Nixpacks identify this as a Python project
# The actual requirements are in backend/requirements-railway.txt

# Core dependencies for deployment
flask==3.1.1
flask-cors==6.0.1
requests==2.32.4
python-dotenv==1.1.1
gunicorn==21.2.0
